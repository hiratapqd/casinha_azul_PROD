<%- include('partials/header') %>
<link rel="stylesheet" href="/styles/styles.css">
<main class="hero">
    <h1>Cadastro de Assistido</h1>
    <form action="/assistido/novo" method="POST" class="form-container">
    <div class="form-row">
        <div class="form-group">
            <label for="nome">Nome Completo:</label>
            <input type="text" id="nome" name="nome" required>
        </div><br>

        <div class="form-group">
            <label for="cpf">CPF:</label>
            <input type="text" id="cpf" name="cpf" placeholder="000.000.000-00" required>
        </div><br>
    </div><br>
    <div class="form-row">
        <div class="form-group">
            <label for="telefone">Telefone/WhatsApp:</label>
            <input type="tel" id="telefone" name="telefone">
        </div><br>

        <div class="form-group">
            <label for="email">E-mail:</label>
            <input type="email" id="email" name="email">
        </div><br>
    </div><br>
        <button type="submit" class="btn-enviar">Finalizar Cadastro do assistido</button>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>


<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    const formAssistido = document.querySelector('form');
    formAssistido.addEventListener('submit', async (e) => {
        e.preventDefault(); 

        // 1. Converte o formulário em um objeto JavaScript simples
        const formData = new FormData(formAssistido);
        const dados = Object.fromEntries(formData.entries());

        try {
            // 2. Envia como JSON com o Header correto
            const resposta = await fetch('/assistido/novo', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(Object.fromEntries(new FormData(formAssistido))) // Transforma o objeto em string JSON
            });

            if (!resposta.ok) throw new Error('Erro na rede');

            const resultado = await resposta.json();

            if (resultado.status === 'existente') {
                Swal.fire({
                    title: 'CPF já cadastrado!',
                    html: `O assistido <b>${resultado.nome}</b> já possui cadastro na Casinha Azul.`,
                    icon: 'info',
                    confirmButtonText: 'Entendido',
                    confirmButtonColor: '#007bff'
                });
            } else if (resultado.status === 'sucesso') {
                Swal.fire({
                    title: '✅ Sucesso!',
                    text: 'Assistido cadastrado com sucesso.',
                    icon: 'success',
                    showCancelButton: true,
                    confirmButtonText: 'Novo Cadastro',
                    cancelButtonText: 'Ir para o Início'
                }).then((result) => {
                    if (result.isConfirmed) {
                        window.location.reload();
                    } else {
                        window.location.href = '/';
                    }
                });
            } else {
                Swal.fire('Erro!', 'Ocorreu um erro ao salvar o assistido.', 'error');
            }
        } catch (error) {
            console.error('Erro detalhado:', error);
            Swal.fire('Erro!', 'Não foi possível conectar ao servidor.', 'error');
        }
    });
</script>
</main>
<%- include('partials/footer') %>